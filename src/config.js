// Shared by App.jsx and useOnlineStatus.js. Kept in its own module so the
// connectivity hook can reach the API base without importing App.jsx back.
export const API_BASE = 'http://localhost:5000';
